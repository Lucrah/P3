<UserControl x:Class="P3.Views.ResultScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:P3.Views"
             mc:Ignorable="d" 
             d:DesignHeight="901.243" d:DesignWidth="1340" Height="901.243" Width="1340" VerticalAlignment="Top" HorizontalAlignment="Left">
    <ListView x:Name="SearchResults6" Background="#FF2C2C2C" BorderThickness="0">
        <ListView.ItemTemplate>
            <DataTemplate>
                <WrapPanel>
					<TextBlock Text="{Binding SearchResults_Address}" FontWeight="Bold" />
					<TextBlock Text=" (" />
                    <TextBlock Text="{Binding Adress}" FontWeight="Bold" />
                    <TextBlock Text=" (" />
                    <TextBlock Text="{Binding Size}" FontWeight="Bold" />
					<TextBlock Text=" (" />
                    <TextBlock Text="{Binding Liggetid}" />
                    <TextBlock Text=" (" />
					<TextBlock Text="{Binding YearBuilt}"/>
                </WrapPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Hustype"/>
                <GridViewColumn Header="Adresse"/>
                <GridViewColumn Header="Størrelse"/>
				<GridViewColumn Header="Pris"/>
				<GridViewColumn Header="Liggetid"/>
				<GridViewColumn Header="Solgt/Til Salg"/>
            </GridView>
        </ListView.View>
    </ListView>

</UserControl>
