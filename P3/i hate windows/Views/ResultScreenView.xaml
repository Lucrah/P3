<UserControl x:Class="P3.Views.ResultScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:P3.Views"
             mc:Ignorable="d" 
             d:DesignHeight="901.243" d:DesignWidth="1340" Height="901.243" Width="1340" VerticalAlignment="Top" HorizontalAlignment="Left">
    <ListView ItemsSource="{Binding Path=SearchResults}" Background="#FF2C2C2C" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.CanContentScroll="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <WrapPanel>
					<TextBlock x:Name="SearchResults_Address" FontWeight="Bold" />
					<TextBlock Text=" (" />
                </WrapPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Hustype" DisplayMemberBinding="{Binding Path=PropertyType}"/>
                <GridViewColumn Header="Adresse" DisplayMemberBinding="{Binding Path=Address}"/>
                <GridViewColumn Header="Størrelse" DisplayMemberBinding="{Binding Path=Size}"/>
				<GridViewColumn Header="Pris" DisplayMemberBinding="{Binding Path=Price}"/>
				<GridViewColumn Header="Liggetid" DisplayMemberBinding="{Binding Path=LieTime}"/>
				<GridViewColumn Header="Solgt/Til Salg" DisplayMemberBinding="{Binding Path=ForSale}"/>
                <GridViewColumn Header="Byggeår" DisplayMemberBinding="{Binding Path=YearBuilt}"/>
            </GridView>
        </ListView.View>
    </ListView>

</UserControl>
